{
  "build": {
    "env": {
      "PYTHON_VERSION": "3.9.11"
    },
    "commands": [
      "pip install -r requirements.txt"
    ]
  },
  "start": "gunicorn src.components.app:app --bind 0.0.0.0:$PORT"
}
